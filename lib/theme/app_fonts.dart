/// Font families bundled in assets/fonts.
class AppFonts {
  AppFonts._();

  /// Warm, organic serif for the wordmark and headlines.
  static const String display = 'Fraunces';

  /// Friendly sans-serif for body copy and UI text (the app default).
  static const String body = 'PlusJakartaSans';
}
